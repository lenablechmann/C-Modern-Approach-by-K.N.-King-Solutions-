#include <stdio.h>

int main(void)
{
  // prints out -1 if i < j, 0 if equal and 1 if i>j. so proud
  int i = 3, j = 4;
  printf_s("%d\n", (i > j) + (i == j) - (i <= j));
  return 0;


    return 0;
}
